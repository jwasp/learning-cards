import React, {useState} from 'react';
import './Save.css';

function Save(props) {

    return (
        <div>
            <button>save</button>
            <button>cancel</button>
        </div>
    )
}

export default Save;